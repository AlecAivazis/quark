{
    "name": "quark-native",
    "version": "0.0.0",
    "scripts": {
      "build": "quark-build"
    },
    "devDependencies": {
        "quark-core": "^0.0.0",
        "quark-build": "^0.0.0"
    }
}
