{
    "name": "quark-web",
    "version": "0.0.0",
    "module": "build/index.js",
    "scripts": {
      "build": "quark-build"
    },
    "devDependencies": {
        "quark-core": "^0.0.0",
        "quark-build": "^0.0.0"
    }
}
