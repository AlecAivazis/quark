{
	"name": "quark",
  "author": "Alec Aivazis",
	"private": true,
  "description": "a react component library",
  "scripts": {
		"test": "lerna run test",
		"publish": "lerna publish",
		"build:module": "pwd",
		"build": "lerna exec --parallel -- npm run build"
  },
	"license": "MIT",
  "devDependencies": {
    "lerna": "^2.0.0"
  }
}
